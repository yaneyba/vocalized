{
  "name": "vocalized-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "workspace/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:vocalized": "npm run dev --workspace vocalized",
    "dev:vocalized-admin": "npm run dev --workspace vocalized-admin",
    "dev:api": "npm run dev --workspace vocalized-api"
  }
}

